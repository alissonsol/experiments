{
  "name": "mevir",
  "version": "0.2.1",
  "description": "Chrome extension for page content analysis and risk scoring",
  "scripts": {
    "build": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "clean": "rimraf dist",
    "pack": "npm run build && node ./scripts/package-extension.js",
    "build:readable": "webpack --mode development"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "copy-webpack-plugin": "^12.0.2",
    "rimraf": "^5.0.5",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}

